# Request the users name
# Store the users name in a variable called user_name
# Request the users age
# Store the users age in a variable called user_age
# Request the users house number
# Store the users house number in a variable called house_number
# Request the users street name
# Store the users street name in variable called street_name
# Print a single sentence containing all the data of the user

user_name = input("What is your name: ")
name = user_name   
user_age = input("What is your age: ")
age = user_age
house_number = input("What is your house number: ")
street_name = input("What is your street name: ")
print("Hello this is " + name + " and he is " + age +  " years old and lives at number "
       + house_number + " on "+ street_name ) 